name: Node.js CI

on:
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js 26
      uses: actions/setup-node@v4
      with:
        node-version: 26.x
    - name: Diagnose npm config
      run: npm config list; cat ~/.npmrc 2>/dev/null || true; cat /etc/npmrc 2>/dev/null || true
    - name: Clear any injected npm auth
      run: npm config delete //registry.npmjs.org/:_authToken --userconfig ~/.npmrc || true
    - run: npm ci
    - run: npm run lint

  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x, 24.x, 26.x]

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
    - name: Diagnose npm config
      run: npm config list; cat ~/.npmrc 2>/dev/null || true; cat /etc/npmrc 2>/dev/null || true
    - name: Clear any injected npm auth
      run: npm config delete //registry.npmjs.org/:_authToken --userconfig ~/.npmrc || true
    - run: npm ci
    - run: npm run build --if-present
    - run: npm test

  docker:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3
      - name: Setup docker builds
        uses: docker/setup-buildx-action@v3

      - name: Login to ghcr
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{github.repository_owner}}
          password: ${{secrets.GITHUB_TOKEN}}

      - name: Docker meta
        id: tekton-lint
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/ibm/tekton-lint
          tags: |
            type=schedule
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.tekton-lint.outputs.tags }}
          labels: ${{ steps.tekton-lint.outputs.labels }}
